extends _layout
block vars
  - metas = {}
  - metas.title = 'firestore'
block append meta
  +meta(metas)
block title
  title #{metas.title}
block body
  .container
    button#login Login
    button#logout Logout

    h2 chatApp
    form#form(action="")
      input#input(type="text")
      input(type="submit")
    button#btn Button
    ul#ul